{
  "name": "desktop-readiness-probe",
  "version": "1.1.0",
  "description": "Check that an interactive macOS desktop session can launch a configured application",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "setup": "node src/main.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
